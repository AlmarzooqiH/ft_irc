# Architecture Diagram - IRC Authentication System

## 🏗️ System Architecture

```
┌────────────────────────────────────────────────────────────────────────┐
│                              IRC CLIENT                                │
│                   (WeeChat, HexChat, netcat, etc.)                     │
└──────────────────────────────┬─────────────────────────────────────────┘
                               │
                               │ TCP Connection
                               │ Port 6667
                               │
┌──────────────────────────────▼─────────────────────────────────────────┐
│                           IRC SERVER                                   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                      Server Class                               │  │
│  │                                                                 │  │
│  │  • pollfd *clients - Array of client connections               │  │
│  │  • map<int, Client> clientMap - Client state storage           │  │
│  │  • map<int, string> clientBuffer - Message buffering           │  │
│  │  • string password - Server password                           │  │
│  │                                                                 │  │
│  │  ┌───────────────────────────────────────────────────────┐    │  │
│  │  │ Main Event Loop (start())                             │    │  │
│  │  │                                                        │    │  │
│  │  │  1. poll() - Wait for client activity                 │    │  │
│  │  │  2. accept() - Accept new connections                 │    │  │
│  │  │  3. recv() - Receive data from clients                │    │  │
│  │  │  4. Buffer incomplete messages                        │    │  │
│  │  │  5. Extract complete messages (find \r\n)             │    │  │
│  │  │  6. handleMessage() - Process each message            │    │  │
│  │  └────────────────────┬──────────────────────────────────┘    │  │
│  │                       │                                        │  │
│  │                       ▼                                        │  │
│  │  ┌───────────────────────────────────────────────────────┐    │  │
│  │  │ handleMessage()                                       │    │  │
│  │  │                                                        │    │  │
│  │  │  • Parse raw message using Message class              │    │  │
│  │  │  • Extract: command + parameters                      │    │  │
│  │  │  • Validate message format                            │    │  │
│  │  │  • Call processCommand()                              │    │  │
│  │  └────────────────────┬──────────────────────────────────┘    │  │
│  │                       │                                        │  │
│  │                       ▼                                        │  │
│  │  ┌───────────────────────────────────────────────────────┐    │  │
│  │  │ processCommand()                                      │    │  │
│  │  │                                                        │    │  │
│  │  │  CAP  → Capability negotiation                        │    │  │
│  │  │  PASS → Authenticate password ────┐                   │    │  │
│  │  │  NICK → Validate & set nickname ──┼─┐                 │    │  │
│  │  │  USER → Set user info ────────────┘ │                 │    │  │
│  │  │                                      │                 │    │  │
│  │  │  Registration Check:                 │                 │    │  │
│  │  │    if (PASS + NICK + USER all set) ─┘                 │    │  │
│  │  │    → sendWelcomeMessages()                            │    │  │
│  │  └───────────────────────────────────────────────────────┘    │  │
│  │                                                                 │  │
│  │  ┌───────────────────────────────────────────────────────┐    │  │
│  │  │ Helper Methods                                        │    │  │
│  │  │                                                        │    │  │
│  │  │  • sendResponse() - Send data to client               │    │  │
│  │  │  • sendNumericReply() - Format & send numeric         │    │  │
│  │  │  • sendWelcomeMessages() - Send 001-004               │    │  │
│  │  │  • isNicknameValid() - RFC 2812 validation            │    │  │
│  │  │  • isNicknameInUse() - Uniqueness check               │    │  │
│  │  └───────────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                      Message Class                              │  │
│  │                                                                 │  │
│  │  • string prefix - Optional message origin                     │  │
│  │  • string command - IRC command                                │  │
│  │  • vector<string> parameters - Up to 15 params                 │  │
│  │  • bool valid - Parsing success flag                           │  │
│  │                                                                 │  │
│  │  ┌───────────────────────────────────────────────────────┐    │  │
│  │  │ parse() - RFC 2812 Compliant Parser                   │    │  │
│  │  │                                                        │    │  │
│  │  │  1. Extract prefix (if starts with ':')               │    │  │
│  │  │  2. Extract command (required)                        │    │  │
│  │  │  3. Extract parameters:                               │    │  │
│  │  │     - Regular params (space-separated)                │    │  │
│  │  │     - Trailing param (after ':')                      │    │  │
│  │  │  4. Validate format                                   │    │  │
│  │  └───────────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                      Client Class                               │  │
│  │                                                                 │  │
│  │  Data:                                                          │  │
│  │  • string nickname - Client nickname                           │  │
│  │  • string username - Client username                           │  │
│  │  • string realname - Real name                                 │  │
│  │  • string hostname - Hostname (localhost)                      │  │
│  │                                                                 │  │
│  │  State Flags:                                                   │  │
│  │  • bool passwordAuthenticated ← PASS received                  │  │
│  │  • bool nicknameSet ← NICK received                            │  │
│  │  • bool userSet ← USER received                                │  │
│  │                                                                 │  │
│  │  ┌───────────────────────────────────────────────────────┐    │  │
│  │  │ isFullyRegistered()                                   │    │  │
│  │  │                                                        │    │  │
│  │  │  return passwordAuthenticated &&                      │    │  │
│  │  │         nicknameSet &&                                │    │  │
│  │  │         userSet;                                      │    │  │
│  │  └───────────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Message Flow Diagram

```
┌──────────────┐
│    CLIENT    │
└──────┬───────┘
       │
       │ PASS test123\r\n
       │ NICK john\r\n
       │ USER john 0 * :John Doe\r\n
       ▼
┌──────────────────────────────────────────────────────────┐
│ TCP Socket recv()                                        │
│ Buffer: "PASS test123\r\nNICK john\r\nUSER john..."      │
└──────┬───────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│ clientBuffer[fd] Accumulation                            │
│ Append new data, search for \r\n delimiters              │
└──────┬───────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│ Extract Complete Messages                                │
│ Message 1: "PASS test123"                                │
│ Message 2: "NICK john"                                   │
│ Message 3: "USER john 0 * :John Doe"                     │
└──────┬───────────────────────────────────────────────────┘
       │
       │ For each message...
       ▼
┌──────────────────────────────────────────────────────────┐
│ handleMessage(client, "PASS test123")                    │
│   ├─> Message.parse("PASS test123")                      │
│   │     • command = "PASS"                               │
│   │     • params = ["test123"]                           │
│   └─> processCommand(clientFd, "PASS", ["test123"])      │
│         ├─> Validate password                            │
│         ├─> client.setPasswordAuthenticated(true)        │
│         └─> Check registration → Not yet complete        │
└──────┬───────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│ handleMessage(client, "NICK john")                       │
│   ├─> Message.parse("NICK john")                         │
│   │     • command = "NICK"                               │
│   │     • params = ["john"]                              │
│   └─> processCommand(clientFd, "NICK", ["john"])         │
│         ├─> Validate nickname format                     │
│         ├─> Check uniqueness                             │
│         ├─> client.setNickname("john")                   │
│         ├─> client.setNicknameSet(true)                  │
│         └─> Check registration → Not yet complete        │
└──────┬───────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│ handleMessage(client, "USER john 0 * :John Doe")         │
│   ├─> Message.parse("USER john 0 * :John Doe")           │
│   │     • command = "USER"                               │
│   │     • params = ["john", "0", "*", "John Doe"]        │
│   └─> processCommand(clientFd, "USER", [...])            │
│         ├─> Validate parameter count                     │
│         ├─> client.setUsername("john")                   │
│         ├─> client.setRealname("John Doe")               │
│         ├─> client.setUserSet(true)                      │
│         └─> Check registration → COMPLETE! ✅             │
│             └─> sendWelcomeMessages(clientFd)            │
└──────┬───────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│ sendWelcomeMessages()                                    │
│   ├─> sendNumericReply(clientFd, "001", ":Welcome...")   │
│   ├─> sendNumericReply(clientFd, "002", ":Your host...") │
│   ├─> sendNumericReply(clientFd, "003", ":Created...")   │
│   └─> sendNumericReply(clientFd, "004", "HAI 1.0 o o")   │
└──────┬───────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────┐
│ send() to TCP Socket                                     │
│ :HAI 001 john :Welcome to the HAI IRC Network...\r\n     │
│ :HAI 002 john :Your host is HAI, running version 1.0\r\n │
│ :HAI 003 john :This server was created Nov 23 2025\r\n   │
│ :HAI 004 john HAI 1.0 o o\r\n                            │
└──────┬───────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│    CLIENT    │
│  ✅ LOGGED IN │
└──────────────┘
```

---

## 🎭 State Machine Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    Client Connection States                     │
└─────────────────────────────────────────────────────────────────┘

     ┌──────────────────┐
     │  DISCONNECTED    │
     └────────┬─────────┘
              │ accept()
              ▼
     ┌──────────────────┐
     │   CONNECTED      │  State: [F, F, F]
     │                  │  - passwordAuthenticated: false
     │  Waiting for     │  - nicknameSet: false
     │  PASS command    │  - userSet: false
     └────────┬─────────┘
              │ PASS <correct>
              ▼
     ┌──────────────────┐
     │  AUTHENTICATED   │  State: [T, F, F]
     │                  │  - passwordAuthenticated: true ✅
     │  Waiting for     │  - nicknameSet: false
     │  NICK and USER   │  - userSet: false
     └────────┬─────────┘
              │
              ├─────────┬─────────┐
              │         │         │
    NICK      │         │         │      USER
    received  │         │         │      received
              ▼         │         ▼
     ┌──────────────────┐       ┌──────────────────┐
     │ NICK SET         │       │  USER SET        │
     │                  │       │                  │
     │ State: [T, T, F] │       │  State: [T, F, T]│
     │ Waiting for USER │       │  Waiting for NICK│
     └────────┬─────────┘       └────────┬─────────┘
              │                          │
              │ USER                     │ NICK
              │ received                 │ received
              │         ┌────────────────┘
              │         │
              ▼         ▼
     ┌──────────────────────────────┐
     │    FULLY REGISTERED          │  State: [T, T, T]
     │                              │  - passwordAuthenticated: true ✅
     │  All credentials set         │  - nicknameSet: true ✅
     │  Welcome messages sent       │  - userSet: true ✅
     │  Ready for IRC commands      │
     └──────────────────────────────┘
                    │
                    │ Normal IRC operations
                    │ (JOIN, PRIVMSG, PART, etc.)
                    ▼
            ┌───────────────┐
            │  Connected &  │
            │  Operational  │
            └───────────────┘
```

---

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    Security Layers                            │
└──────────────────────────────────────────────────────────────┘

Layer 1: Password Authentication
┌────────────────────────────────────────────────────────────┐
│  PASS command                                              │
│  ✓ Must be first command (before NICK/USER)                │
│  ✓ Password comparison                                     │
│  ✓ Wrong password → immediate disconnect                   │
│  ✓ Cannot re-authenticate                                  │
└────────────────────────────────────────────────────────────┘
                          ↓
Layer 2: Nickname Validation
┌────────────────────────────────────────────────────────────┐
│  NICK command                                              │
│  ✓ Format validation (RFC 2812)                            │
│    - First char: letter or special                         │
│    - Other chars: letter, digit, special, dash             │
│    - Length: 1-9 characters                                │
│  ✓ Uniqueness check                                        │
│    - Iterate through all connected clients                 │
│    - Reject if nickname already exists                     │
└────────────────────────────────────────────────────────────┘
                          ↓
Layer 3: User Information
┌────────────────────────────────────────────────────────────┐
│  USER command                                              │
│  ✓ Parameter validation (4 required)                       │
│  ✓ Cannot be sent twice                                    │
│  ✓ Stores username and realname                            │
└────────────────────────────────────────────────────────────┘
                          ↓
Layer 4: Registration Check
┌────────────────────────────────────────────────────────────┐
│  isFullyRegistered()                                       │
│  ✓ All three flags must be true                            │
│  ✓ Automatic welcome messages                              │
│  ✓ Client marked as operational                            │
└────────────────────────────────────────────────────────────┘
```

---

**Complete IRC authentication system with robust security and state management! 🎉**
